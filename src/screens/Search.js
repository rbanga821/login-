import React from 'react';
import {View, Text} from 'react-native';
const Search = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#ffff00',
      }}>
      <Text style={{fontSize: 90}}>Search</Text>
    </View>
  );
};

export default Search;
